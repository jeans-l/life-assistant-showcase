# Runtime Loop

统一主循环：Trigger → Entry → Route → Context Load → Domain Judgment → Validation → State Recheck → Policy Decision → Execution or Handoff → Writeback Decision → Return Sync → Correction。

详细说明见 `../docs/04-assistant-runtime-and-architecture.md`。
